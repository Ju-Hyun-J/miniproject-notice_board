spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/board_db
    username: boarddb
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
